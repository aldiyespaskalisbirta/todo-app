generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Todos {
  id    String @id @default(cuid())
  title String
  status Status @default(TODO)
}

enum Status {
  TODO
  IN_PROGRESS
  DONE
}
